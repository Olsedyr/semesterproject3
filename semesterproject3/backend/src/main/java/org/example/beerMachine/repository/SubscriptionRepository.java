package org.example.beerMachine.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

//@Repository
public interface SubscriptionRepository// extends JpaRepository<SubscriptionValuesDTO, Long>
{
    // Additional custom query methods or configurations can be added here if needed
}